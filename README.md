# workshop-3

მოკლე აღწერა: ამ სამუშაოზე ივარჯიშებ **if/else** და **switch** კონსტრუქციებში, ასევე ისწავლი, როგორ აიღო მონაცემი მომხმარებლისგან **prompt**-ის საშუალებით.

ყველა დავალება იწერება ერთ ფაილში — `app.js`. თითოეული დავალებისთვის ცალკე ფუნქციაა მოცემული, სადაც უნდა იპოვო `TODO` კომენტარი და დაასრულო კოდი.

---

## 📋 დავალება 1: ლუწი თუ კენტი

**რა უნდა გააკეთო:**

- გახსენი: `app.js`
- იპოვე `checkEvenOrOdd` ფუნქციაში `TODO` კომენტარი
- დაწერე `if/else`, რომელიც დაბეჭდავს `even` ლუწი რიცხვებისთვის და `odd` კენტი რიცხვებისთვის

```javascript
function checkEvenOrOdd(number) {
  // TODO: დაწერე if/else კონსტრუქცია
}
```

---

## 📋 დავალება 2: ასაკის შემმოწმებელი (prompt + if/else)

**რა უნდა გააკეთო:**

- გახსენი: `app.js`
- `checkAge` ფუნქციაში გამოიყენე `prompt("შეიყვანე შენი ასაკი:")`, რომ მომხმარებელმა შეიყვანოს ასაკი
- დაწერე `if/else`, რომელიც:
  - თუ ასაკი `< 13` — დაბეჭდავს `"ბავშვი ხარ"`
  - თუ ასაკი `13`-დან `17`-მდე (ჩათვლით) — დაბეჭდავს `"თინეიჯერი ხარ"`
  - თუ ასაკი `18` ან მეტი — დაბეჭდავს `"ზრდასრული ხარ"`

```javascript
function checkAge() {
  let age = prompt("შეიყვანე შენი ასაკი:");
  age = Number(age);

  // TODO: დაწერე if/else კონსტრუქცია
}
```

**მინიშნება:** `prompt`-იდან მიღებული მნიშვნელობა ყოველთვის სტრიქონია (string), ამიტომ `Number()`-ით გადაგვყავს რიცხვად.

---

## 🍕 დავალება 3: პიცის ფასის კალკულატორი (prompt + if/else + switch)

**რა უნდა გააკეთო:**

- გახსენი: `app.js`
- `pizzaPrice` ფუნქციაში გამოიყენე ორი `prompt`:
  - `prompt("აირჩიე ზომა: small, medium, large")`
  - `prompt("გინდა დამატებითი ყველი? yes/no")`
- `switch`-ით დაადგინე ფასი ზომაზე დაყრდნობით:
  - `small` → `10`
  - `medium` → `15`
  - `large` → `20`
  - სხვა შემთხვევაში → დაბეჭდე `"არასწორი ზომა"` და შეწყვიტე ფუნქცია (`return`)
- შემდეგ `if/else`-ით დაამატე ფასს `3`, თუ მომხმარებელმა `"yes"` უპასუხა დამატებით ყველზე
- ბოლოს დაბეჭდე: `"შენი პიცის ფასია: X ლარი"`

```javascript
function pizzaPrice() {
  //აქ ფრომფთები ჩასვი
  let price;

  // TODO: switch — დაადგინე price ზომის მიხედვით
  // სხვა შემთხვევაში დაბეჭდე "არასწორი ზომა" და გააკეთე return

  // TODO: if/else — დაამატე 3, თუ extraCheese === "yes"

  console.log(`შენი პიცის ფასია: ${price} ლარი`);
}
```

---
